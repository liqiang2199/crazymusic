<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="actionBarSize">48dp</item>
        <item name="android:textColor">@color/colorCommonText</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.AppBarOverlay" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
    </style>
    <!--布局根节点样式-->
    <style name="coordinatorLayout_Style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/bgColor</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:orientation">vertical</item>
    </style>

    <!--评分样式-->
    <style name="RattingStyle" parent="@android:style/Widget.RatingBar">
        <!-- 定义星星图片 -->
        <item name="android:progressDrawable">@drawable/ratin_bar_style</item>
        <item name="android:minHeight">22dip</item>
        <item name="android:maxHeight">22dip</item>
    </style>
    <!--大图样式-->
    <style name="AppTheme.transparent2" parent="AppTheme">
        <item name="android:windowBackground">@color/transparent2</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!--弹出选择方式-->
    <style name="mystyle" parent="android:Animation">
        <!-- 进入时的动画 -->
        <item name="@android:windowEnterAnimation">@anim/dialog_bottom_in</item>
        <!-- 退出时的动画 -->
        <item name="@android:windowExitAnimation">@anim/dialog_bottom_out</item>
    </style>
    <!--pop_style_out弹出选择方式-->
    <style name="pop_style_out" parent="android:Animation">
        <!-- 进入时的动画 -->
        <item name="@android:windowEnterAnimation">@anim/anim_dialog_enter</item>
        <!-- 退出时的动画 -->
        <item name="@android:windowExitAnimation">@anim/anim_dialog_enter</item>
    </style>
    <!--底部对话框-->
    <style name="AppTheme.bottomDialog" parent="android:Theme.Dialog">
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item><!--背景透明-->
        <item name="android:windowAnimationStyle">@style/mystyle</item>
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">true</item><!--半透明-->
        <item name="android:backgroundDimEnabled">true</item><!--模糊-->
    </style>

    <!--启动样式-->
    <style name="AppTheme.Splash" parent="AppTheme">
        <item name="android:windowTranslucentStatus" tools:ignore="NewApi">true</item>
        <item name="android:windowBackground">@mipmap/bg_start</item>
    </style>

    <style name="font_size_14_colorCommonAssist">
        <item name="android:textSize">@dimen/text_14</item>
        <item name="android:textColor">@color/colorCommonAssist</item>
    </style>
    <style name="font_size_25_black">
        <item name="android:textSize">@dimen/text_25</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
